dist: "bionic"
language: "python"
python:
  - "3.7"
install:
  - pip install -r requirements.txt
script:
  - safety check
  - black . --check
  - coverage run --source=tests -m pytest tests
after_success: coveralls
